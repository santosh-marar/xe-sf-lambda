Learning or not understandable code

! error
No inputs were found in config file '/home/santosh-marar/Documents/node-ts/tsconfig.json'. Specified 'include' paths were '["src/**/*"]' and 'exclude' paths were '["coverage","dist","node_modules"]'.ts

solution
Run TypeScript Compilation Manually: To see if TypeScript can find your files, try running:

```bash
npx tsc --noEmit
```
